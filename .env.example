# Vite 환경변수는 반드시 VITE_ 접두어가 있어야 클라이언트 번들에 포함됩니다.
# 로컬에서 API 서버 주소를 지정하세요.
#
# 사용처: src/api/client.ts (import.meta.env.VITE_API_BASE_URL)
VITE_API_BASE_URL=https://s1.takealook.my
VITE_ENABLE_NICKNAME_UPDATE=true
VITE_REGACY_LOGIN=false

# 인증 플로우 디버깅: true 설정 시 콘솔에 요청/리다이렉트 로그 출력
VITE_DEBUG_AUTH_FLOW=false
